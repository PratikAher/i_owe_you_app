import 'package:flutter/material.dart';
import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:provider/provider.dart';

class PplList extends StatefulWidget {
  @override
  _PplListState createState() => _PplListState();
}

class _PplListState extends State<PplList> {
  @override
  Widget build(BuildContext context) {
    //
    // final ppl = Provider.of<QuerySnapshot>(context);

    // print(ppl.documents);
    // for (var i in ppl.documents) {
    //   print(i.data);
    // }

    return Container();
  }
}
